// get_google_search.js

// serpapi
const fetch = require('node-fetch');
const SERPAPI_APIKEY = process.env.SERPAPI_APIKEY;

const get_google_search = async ({ query }) => {
  try {
    console.log("get_google_search function was called");
    const serpApiUrl = `https://serpapi.com/search?q=${encodeURIComponent(query)}&api_key=${SERPAPI_APIKEY}`;
    const response = await fetch(serpApiUrl, {method: 'GET'});
    const data = await response.json();

    return data.organic_results;
  }
  catch (error) {
    console.log("Error:", error.message);
    return { error: error.message };
  }
}

module.exports = { get_google_search };

/*
{
  "name": "get_google_search",
  "description": "This function connects to the Google Search API to retrieve search results from Google. Useful for looking up information about virtually anything.",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "The Google search query, optimized to target the best results for the inquiry in question."
      }
    },
    "required": [
      "query"
    ]
  }
}
*/